public class CalculatePowerOfNumber {
    public static void main(String[] args) {
        int base = 10;
        int exponent = 4;
        double powerNumber = Math.pow(base,exponent);
        System.out.println("power of number is:"+powerNumber);
    }
}
